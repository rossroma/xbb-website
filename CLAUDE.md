# XBB Website 项目规则

## 1. 本地代理绕过

本机配置了代理，直接访问本地接口（localhost/127.0.0.1）会被代理拦截。**所有 curl 请求本地服务时必须绕过代理**：

```bash
ALL_PROXY="" http_proxy="" https_proxy="" curl -s -X POST http://localhost:3000/...
```

**规则**：凡是目标地址为 `localhost` 或 `127.0.0.1` 的 curl 命令，必须加 `ALL_PROXY="" http_proxy="" https_proxy=""` 前缀。不要使用 `--noproxy '*'` 或 `env -u` 方式，统一用上述前缀。

## 2. Bug 修复流程 —— 先查证，后下结论

在修复 bug 时，**禁止在没有完整掌握信息链的情况下直接给出解决方案**。必须遵循以下步骤：

### 信息链查证清单（按顺序执行）

1. **复现问题**：确认能稳定复现 bug，记录复现步骤和实际现象
2. **定位入口**：从用户可见的错误现象出发，沿调用链逐层向下追踪（前端 → API → Service → 数据层）
3. **阅读相关代码**：使用 `codegraph_context` + `codegraph_trace` 掌握完整调用路径，不要只看一个文件就下结论
4. **确认根因**：在修改代码之前，用日志/断点/curl 测试验证你对根因的假设
5. **评估影响范围**：用 `codegraph_impact` 检查修改会影响哪些调用方
6. **给出方案并修复**：在上一步确认后，才给出修复方案并实施
7. **验证修复**：用复现步骤重新测试，确认问题已彻底解决

### 禁止行为

- ❌ 看完一个文件就推断根因，不追踪完整调用链
- ❌ 在没有用 curl/Bash 实际验证的情况下假设 API 行为
- ❌ 修改代码后不验证修复效果就声称"已解决"
- ❌ 多次在同一个问题上反复尝试不同方案（说明信息链查证不充分）

## 3. 部署流程规则 —— 先检查，后推送

**因为 develop/main 分支推送后会自动触发 CI/CD 部署，所以任何向这两个分支的推送都必须经过部署前检查。** 检查由 pre-push 钩子自动执行，无需手动调用。

### 触发条件

以下场景在推送至 `develop` 或 `main` 分支时，自动触发检查：

- 修改 `.github/workflows/` 下的 CI/CD 配置
- 修改 `Dockerfile`（前端或后端）
- 修改 `docker-compose.yml`
- 修改 `scripts/` 目录下的部署/验证脚本
- 新增/修改环境变量

### 检查机制

1. **pre-push 钩子自动检测**：推送时检测到上述文件变更，自动运行 `scripts/deploy-check.sh`
2. **自检未通过 → 推送被阻断**，必须在本地修复后重新推送
3. **紧急情况可跳过**：`git push --no-verify`（不推荐，仅限紧急修复）

### 核心原则

**任何 CI/CD 相关变更，在推送前必须通过部署前自检。** 自检清单见 `.claude/skills/deploy/SKILL.md`。

### 关键规则速查

| 场景 | 规则 |
|---|---|
| 涉及外部服务（ACR/SSH） | 先在服务器上手动验证可用性，再提交 CI 配置 |
| 新增/修改环境变量 | 在所有引用处同步更新（deploy.yml、docker-compose.yml、部署脚本） |
| Dockerfile 构建问题 | 一次性检查所有可能的缺失项，列出完整性清单，不要改一个点就推 |
| 方案变更 | 先做可行性验证 + 评估矩阵，再做一半发现不行就换 |
| 第 3 次同类修改 | 必须停下来，做反思检查，写设计文档，再提交 |
| 下午 5 点后 | 禁止提交 CI/CD 大规模变更 |
| 同类小修改 | 合并为一个提交再推送，不要让 CI 重复跑 |